# ResearchFlow AI — dependencies for the ACTIVE code path
# (agents/, pipelines/, app/). The monitoring/, scheduler/, nlp/, retrieval/,
# and vector_store/ folders are not imported by the running app — see analysis.

# --- UI ---
streamlit>=1.35
plotly>=5.21
pandas>=2.0

# --- config / system ---
pyyaml>=6.0
psutil>=6.0

# --- retrieval + NLP (real mode) ---
arxiv>=2.1
numpy>=1.26
scikit-learn>=1.3
hdbscan>=0.8.33
umap-learn>=0.5.5          # imported as `umap`
sentence-transformers>=2.7  # pulls in torch (large download)

# --- LLM orchestration (real mode) ---
langchain>=0.2
langchain-openai>=0.1
openai>=1.30
pydantic>=2.0

# torch is installed transitively by sentence-transformers. For a CPU-only,
# smaller install, see https://pytorch.org for the right CPU wheel index.
